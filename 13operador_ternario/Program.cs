// Variável
double media = 8;

// Operador ternário
//Console.WriteLine(media >= 7 ? "Aprovado(a)" : "Reprovado(a)");

string situacao = media >= 7 ? "Aprovado(a)" : "Reprovado(a)";
Console.WriteLine(situacao);


